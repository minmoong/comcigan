{
  "name": "comcigan",
  "version": "2.0.3",
  "description": "컴시간 데이터 추출 라이브러리",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepack": "npm run clean && npm run build",
    "build": "tsc",
    "clean": "tsc --build --clean",
    "dev": "nodemon --exec ts-node src/index.ts"
  },
  "author": "minmoong",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.8.8",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "iconv-lite": "^0.6.3"
  }
}
